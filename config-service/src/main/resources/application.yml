server:
  port: 8888
  application:
       name: config-service

spring:
  cloud:
    config:
      server:
            git:
               uri: https://github.com/robbyA/reservation-config-repo  
               skipSslValidation: true
  devtools:
    livereload:
      enabled: true