server:
  port: 8888

spring:
  profiles:
    active: native
  cloud:
    config:
      server:
           git:
             uri: ~/github/spring-cloud-reservation-project/config
  devtools:
    livereload:
      enabled: true
  application:
     name: config-service

#eureka:
#   client:
#      serviceUrl:
#         defaultZone: http://eureka:8761/eureka